/* ==========================================================================
   N8N WORKFLOW MODAL COMPONENT (DEPRECATED & REMOVED)
   ========================================================================== */

export function renderN8nModal(containerEl) {
  if (containerEl) containerEl.innerHTML = '';
}

export function openN8nModal() {}
export function closeN8nModal() {}
